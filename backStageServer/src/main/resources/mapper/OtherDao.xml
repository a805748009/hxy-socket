<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.business.dao.OtherDao">
    <!-- 所有字段 -->
    <sql id="Base_OnlineDao" >
        id,configName,status,game_name
    </sql>
    <!--END -->

    <!--查询系统设置信息-->
    <select id="selectSomeSystemInfoByName" resultType="java.util.Map">
        select <include refid="Base_OnlineDao" />
        from ${dataBaseName}.systemInfo where game_name = #{gameName} and configName = #{configName}
    </select>

    <update id="updateSystemInfoByName">
        update ${dataBaseName}.systemInfo set status = #{isOpen} where game_name = #{gameName} and configName = #{configName}
    </update>
</mapper>