<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.business.dao.DruidDao">
    <!-- 所有字段 -->
    <!--END -->

    <select id="selectDruidInfoList" resultType="java.util.Map">
        select * from druid_monitor
    </select>

    <delete id="deleteDruidInfoById">
        delete from druid_monitor where id = #{druidId}
    </delete>
</mapper>